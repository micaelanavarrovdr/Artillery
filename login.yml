config:
  target: "https://jsonplaceholder.typicode.com"
  phases:
    - duration: 15
      arrivalRate: 3

scenarios:
  - flow:
      - post:
          url: "/posts"
          json:
            title: "Test"
            body: "Probando Artillery"
            userId: 1
