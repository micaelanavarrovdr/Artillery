config:
  target: "https://jsonplaceholder.typicode.com"
  phases:
    - duration: 60
      arrivalRate: 10   # empieza con 10 usuarios/seg
      rampTo: 50        # va subiendo hasta 50 usuarios/seg

scenarios:
  - flow:
      - get:
          url: "/posts"
